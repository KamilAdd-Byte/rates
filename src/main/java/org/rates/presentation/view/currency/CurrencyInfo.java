package org.rates.presentation.view.currency;

import org.rates.presentation.view.Display;

public class CurrencyInfo {
    private static final String INFO = "select the currency code you are interested in and search for values in option 3:";

    private static final String LINE_SEPARATE = "_________________________________________________________________________________";


    public static void printLineSeparate() {
        Display.displayContent(LINE_SEPARATE);
    }

}
